# Import checker tests (sys.modules surgery, requires subprocess override)
TEST_PATHS="tests/import_check/"
MARKERS=""
TIMEOUT=15
RUN_TIMEOUT=120
VALIDATION_OVERRIDES="subprocess:allow"
EXTRA_FLAGS=""
DESCRIPTION="Import checker tests (sys.modules surgery, requires subprocess override)"
