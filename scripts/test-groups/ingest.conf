# Ingestion pipeline tests (document processing, embedding, orchestration)
TEST_PATHS="tests/ingest/"
MARKERS=""
TIMEOUT=30
RUN_TIMEOUT=300
VALIDATION_OVERRIDES=""
EXTRA_FLAGS=""
DESCRIPTION="Ingestion pipeline tests (document processing, embedding, orchestration)"
